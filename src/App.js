import logo from './logo.svg';
import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        {/*<img src={logo} className="App-logo" alt="logo" />*/}
        Placeholder_for("Inputs = input1, input2, input3", constraints = inputs need to be validated) <br></br>
        Placeholder_for("Refresh button") <br></br>
        Placeholder_for("output = f(inputs)")
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
        
      </header>
      
    </div>
    
  );
}

export default App;